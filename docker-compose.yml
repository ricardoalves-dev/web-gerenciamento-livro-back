services:
  postgres:
    image: postgres
    ports:
      - 5432:5432
    container_name: web_gerenciamentro_livros
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=db
    volumes:
      - ./db_data:/var/lib/postgresql/data